"""KAC pre-production shadow-deployment artifacts.

Portable, CPU-only representation of the deployment described in the paper:
a FastAPI scorer (:mod:`deployment.serve_app`), a shadow-mode harness
(:mod:`deployment.shadow_runner`), and a latency/parameter benchmark
(:mod:`deployment.benchmark_latency`), plus container and Kubernetes manifests.
"""
