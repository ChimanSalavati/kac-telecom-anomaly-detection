# Serving extras for the KAC shadow scorer (on top of the repo's requirements.txt).
# The model itself needs torch + transformers (already in ../requirements.txt).
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
