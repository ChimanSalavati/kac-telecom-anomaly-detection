% Snapshot of the parameter-breakdown numbers in main_kac_paper.tex.
\begin{table}[t]
  \caption{KAC parameter breakdown (from the released checkpoint). The trainable portion is dominated by the last two unfrozen DistilBERT blocks; the KAC-specific head (KPI queries, projections, gated fusion, conv, attention pool, MLP) adds only $\sim$0.14M parameters on top of the frozen Chronos-2 residual extractor (which is used offline and is not part of the deployed detector).}
  \label{tab:deployment_params}
  \small
  \begin{tabular}{l|rr}
    \toprule
    \textbf{Component} & \textbf{Parameters} & \textbf{\% of total} \\
    \midrule
    text\_encoder (frozen lower layers) & 52,187,136 & 78.47\% \\
    text\_encoder (trainable last 2 layers) & 14,175,744 & 21.31\% \\
    KAC fusion + heads (projections, attention, conv, MLP) & 142,337 & 0.21\% \\
    learnable queries (KPI + cls) & 1,088 & 0.00\% \\
    \midrule
    \textbf{Total} & \textbf{66,506,305} & 100.00\\% \\
    \textit{Trainable (last 2 DistilBERT + head)} & \textit{14,319,169} & \textit{21.53\\%} \\
    \bottomrule
  \end{tabular}
\end{table}
