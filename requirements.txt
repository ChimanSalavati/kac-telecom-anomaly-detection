# Core scientific Python
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Tabular detectors
xgboost>=2.0
lightgbm>=4.0

# Deep learning (required for KAC, SOTA baselines, and TSFMs)
torch>=2.0
transformers>=4.30
accelerate>=0.20
einops>=0.7  # tensor rearrange ops used by the DCdetector SOTA baseline

# Time-series foundation model used for residual extraction
# Chronos-2 is loaded via HuggingFace Hub by scripts/compute_chronos_residuals.py.
chronos-forecasting>=1.4
huggingface_hub>=0.20

# Dataset loading (TelecomTS lives on HuggingFace Datasets;
# SpotLight is shipped as a public release on GitHub).
datasets>=2.14
pyarrow>=14.0
requests>=2.31

# Jupyter / notebook execution
jupyter>=1.0
nbformat>=5.7
nbconvert>=7.0
ipykernel>=6.0

# Progress and utilities
tqdm>=4.65
